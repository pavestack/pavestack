apiVersion: v1
kind: ServiceAccount
metadata:
  namespace: {{ .Release.Namespace }}
  name: {{ default (include "service-template-api.fullname" .) .Values.serviceAccount.name }}
  labels:
    {{- include "service-template-api.labels" . | nindent 4 }}
automountServiceAccountToken: false
